<UserControl
    x:Class="Main_UWP.View.ConnectionListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Main_UWP.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="120">

    <StackPanel Grid.Column="0" Margin="2">
        <ListBox x:Name="lb_Connection"
                 Height="100" SelectionChanged="Lb_Connection_SelectionChanged">
        </ListBox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="SearchCommand" Width="60"
                        Content="조회" 
                        Command="{Binding SearchCommand}"
                        Click="SearchCommand_Click"/>
            <!--<Button Grid.Column="1" x:Name="AddCommand" Width="60"
                        Content="추가" 
                        Click="AddCommand_Click"/>-->
        </Grid>
        <!--<ListBox Height="100"
                     ItemsSource="{Binding SelectedList, Mode=TwoWay}">
            </ListBox>-->

    </StackPanel>
</UserControl>
